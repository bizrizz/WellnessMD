EXPO_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# OAuth redirect for Google sign-in. Add this exact URL to Supabase Dashboard → Auth → URL Configuration → Redirect URLs
EXPO_PUBLIC_AUTH_REDIRECT_URL=wellnessmd://auth/callback
# For push notifications: run "npx eas init" to get project ID, or add it here (UUID from expo.dev)
# EXPO_PUBLIC_EAS_PROJECT_ID=your-eas-project-uuid
